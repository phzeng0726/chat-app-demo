name: chat_app_demo
description: A new Flutter project.

publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.17.1 <3.0.0"

dependencies:
  # NOTE: necessary
  cupertino_icons: ^1.0.2
  flutter:
    sdk: flutter
  # NOTE: firebase
  firebase_auth: ^3.3.19
  firebase_core: ^1.17.1
  firebase_storage: ^10.2.18
  cloud_firestore: ^3.1.17
  # NOTE: router
  auto_route: ^4.0.1
  path_provider: ^2.0.11
  # NOTE: log and error code display
  another_flushbar: ^1.10.29
  logger: ^1.1.0
  # NOTE: state management
  flutter_bloc: ^8.0.1
  # NOTE: localizations
  flutter_i18n: ^0.32.4
  flutter_localizations:
    sdk: flutter
  # NOTE: speed up the development process
  dartz: ^0.10.1
  freezed_annotation: ^1.0.0
  json_serializable: ^6.2.0
  rxdart: ^0.27.4
  # NOTE: others
  image_picker: ^0.8.5+3
  flutter_markdown: ^0.6.10+2
  url_launcher: ^6.1.4

dev_dependencies:
  auto_route_generator: null
  bloc_test: ^9.0.3
  build_runner: null
  flutter_lints: ^2.0.0
  flutter_test:
    sdk: flutter
  freezed: ^1.1.0
  injectable_generator: null
  mocktail: ^0.3.0

flutter:
  uses-material-design: true
  assets:
    - assets/i18n/
    - assets/images/blank_profile_picture.png
    - assets/others/app_introduction.md
